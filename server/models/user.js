const mongoose = require('mongoose');
//import user schema
const UserSchema = require('../models/schema/schema');


// module.exports = mongoose.model('User',UserSchema);

module.exports = UserSchema;
